-- 0012 — уведомление о новом сообщении в заявке.
--
-- Отдельным файлом от 0011 по той же причине, что и 0009 от 0008:
-- PostgreSQL не даёт использовать новое значение enum в той же транзакции,
-- в которой оно добавлено, а каждая миграция у нас выполняется одной
-- транзакцией. Здесь значение только добавляется, но следующий файл или
-- код, который на него сошлётся, должен видеть его уже закоммиченным.

alter type notification_kind add value 'message_received';
