/**
 * Имена cookie авторизации.
 *
 * Вынесены в отдельный модуль без единой зависимости намеренно: их читает и
 * proxy.ts, который выполняется на каждый запрос к любой странице. Если
 * брать их из session.ts, вместе с константой в сборку proxy уезжает драйвер
 * PostgreSQL и вся работа с базой — на каждый запрос за картинкой.
 *
 * Здесь нет `import "server-only"`: имя cookie не секрет, а модуль обязан
 * собираться и вне серверного окружения React.
 */
export const ACCESS_COOKIE = "opus_access";
export const REFRESH_COOKIE = "opus_refresh";
